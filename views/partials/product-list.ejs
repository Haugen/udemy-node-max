
<% for (let product of products) { %>
  <%- include('product', {
    title: product.title,
    imageUrl: product.imageUrl,
    description: product.description,
    price: product.price,
    _id: product._id,
    admin: adminView
  }) %>
<% } %>