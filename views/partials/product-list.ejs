
<% for (let product of products) { %>
  <%- include('product', {
    title: product.title,
    imageURL: product.imageURL,
    description: product.description,
    price: product.price,
    id: product.id,
    admin: adminView
  }) %>
<% } %>